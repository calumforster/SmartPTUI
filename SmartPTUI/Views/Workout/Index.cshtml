@using SmartPTUI.Data.DomainModels
@model WorkoutPlan

@for (int i = 0; i < Model.WorkoutWeek.Count; i++)
{
    <h1>Week @(i+1)</h1>
    @await Html.PartialAsync("WorkoutWeek.cshtml", Model.WorkoutWeek[i])
}
