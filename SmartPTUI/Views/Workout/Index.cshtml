@using SmartPTUI.Data.DomainModels
@model WorkoutPlan

    <h1>Workout Weeks:</h1>
@for (int i = 0; i < Model.WorkoutWeek.Count; i++)
{
    @Html.ActionLink("Week "+(i+1), "WorkoutWeek", "Workout", new { id = Model.WorkoutWeek[i].Id })
}
